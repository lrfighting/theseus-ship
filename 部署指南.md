# 盐言互动 · 部署指南

## 部署原理

本项目采用**单服务部署**架构：

- **Express 后端**同时承担 API 服务和静态文件服务
- **前端**（React + Vite）打包后生成 `dist/` 目录，由 Express 通过 `express.static()` 直接托管
- 用户只需访问**一个域名**，例如 `https://yyan.onrender.com`
- API 请求走相对路径 `/api/...`，无需处理跨域（CORS）

```
用户浏览器 ──→ https://yyan.onrender.com
                    │
                    ├── /api/*  → Express API 路由
                    └── /*      → 前端静态文件 (dist/index.html)
```

## 已完成的部署准备

以下修改已包含在当前代码中，无需手动处理：

1. ✅ `server/src/index.ts` 已添加 `express.static()` 和 SPA fallback
2. ✅ `package.json` 已添加 `npm start` 启动脚本
3. ✅ `tsx` 已移入 `dependencies`，生产环境可直接运行 TypeScript
4. ✅ 前端 API 使用相对路径 `/api`，生产环境自动适配同域名

## 部署步骤

### 方案一：Render（推荐，免费）

[Render](https://render.com) 提供免费 Web Service，支持从 GitHub 一键部署。

#### 1. 推送代码到 GitHub

```bash
git add .
git commit -m "prepare for deploy"
git push origin main
```

> 确保 `.env.local` 和 `dist/` 在 `.gitignore` 中（已配置），**不要提交到仓库**。

#### 2. 在 Render 创建 Web Service

1. 登录 [dashboard.render.com](https://dashboard.render.com)
2. 点击 **New +** → **Web Service**
3. 连接你的 GitHub 仓库
4. 填写配置：

| 配置项 | 值 |
|---|---|
| **Name** | `yyan`（自定义） |
| **Runtime** | `Node` |
| **Build Command** | `npm install && npm run build` |
| **Start Command** | `npm start` |
| **Plan** | `Free` |

5. 点击 **Create Web Service**

#### 3. 配置环境变量

进入服务 Dashboard → **Environment** 标签页，添加以下变量：

| Key | Value | 说明 |
|---|---|---|
| `AI_PROVIDER` | `kimi` | AI 提供商 |
| `KIMI_API_KEY` | `sk-kimi-...` | 你的 Kimi Code API Key |
| `KIMI_BASE_URL` | `https://api.kimi.com/coding/v1` | Kimi Code 基础地址 |
| `KIMI_MODEL` | `kimi-for-coding` | 模型名称 |
| `KIMI_TEMPERATURE` | `0.7` | 温度参数（可选） |
| `SERVER_PORT` | `10000` | 服务端口（Render 要求用 10000） |

> ⚠️ **安全提示**：API Key 只在 Render 后台配置，**绝不要写入代码或提交到 Git**。

#### 4. 等待部署完成

Render 会自动执行：
1. `npm install` — 安装依赖
2. `npm run build` — 构建前端到 `dist/`
3. `npm start` — 启动 Express 服务（端口 10000）

部署完成后，Render 会分配一个域名，例如：

```
https://yyan.onrender.com
```

直接分享给用户即可访问。

---

### 方案二：Railway（备选，免费额度）

[Railway](https://railway.app) 也提供免费额度，步骤类似：

1. 登录 Railway，New Project → Deploy from GitHub repo
2. 选择你的仓库
3. 在 **Variables** 中添加与上面相同的环境变量
4. Railway 会自动检测 `package.json` 并执行 `npm run build` + `npm start`
5. 自动生成域名，例如 `https://yyan.up.railway.app`

---

### 方案三：自有服务器（阿里云/腾讯云/VPS）

如果你有自己的云服务器：

```bash
# 1. 克隆代码到服务器
git clone https://github.com/yourname/your-repo.git
cd your-repo

# 2. 安装依赖并构建
npm install
npm run build

# 3. 创建 .env 文件（不要提交到 Git）
cat > .env <<EOF
AI_PROVIDER=kimi
KIMI_API_KEY=sk-kimi-...
KIMI_BASE_URL=https://api.kimi.com/coding/v1
KIMI_MODEL=kimi-for-coding
SERVER_PORT=4000
EOF

# 4. 启动服务
npm start
```

然后配置 Nginx 反向代理（推荐）：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

或使用 PM2 守护进程：

```bash
npm install -g pm2
pm2 start "npm start" --name yyan
pm2 save
pm2 startup
```

---

## 环境变量完整清单

| 变量名 | 必填 | 默认值 | 说明 |
|---|---|---|---|
| `AI_PROVIDER` | ✅ | `mock` | AI 提供商：`kimi` 或 `mock` |
| `KIMI_API_KEY` | 是（当 provider=kimi）| - | Kimi Code API Key |
| `KIMI_BASE_URL` | 否 | `https://api.moonshot.cn/v1` | Kimi API 基础地址 |
| `KIMI_MODEL` | 否 | `kimi-k2-0905-preview` | 模型 ID |
| `KIMI_TEMPERATURE` | 否 | `0.7` | 生成温度 |
| `SERVER_PORT` | 否 | `4000` | 服务监听端口 |
| `CACHE_DIR` | 否 | `./.cache/yyan-ai` | AI 缓存目录 |

---

## 常见问题

### Q1: 部署后页面空白？

检查 Render/Railway 的 Build Logs，确认 `npm run build` 成功执行并生成了 `dist/` 目录。

### Q2: API 请求 404？

确认环境变量中 `AI_PROVIDER` 已设置为 `kimi`（不是 `mock`），且 `KIMI_API_KEY` 正确。

### Q3: 免费套餐休眠？

Render Free 套餐在 15 分钟无访问后会休眠，下次访问需要等待 30 秒左右唤醒。如需保持在线，可使用 [UptimeRobot](https://uptimerobot.com) 每 5 分钟 ping 一次你的网址。

### Q4: 如何更新代码？

本地修改后推送到 GitHub，Render/Railway 会自动重新部署：

```bash
git add .
git commit -m "update"
git push origin main
```

---

## 本地验证（部署前测试）

在本地模拟生产环境：

```bash
# 1. 构建前端
npm run build

# 2. 启动生产服务（使用 .env.local 中的配置）
npm start

# 3. 访问 http://localhost:4000
```

如果本地 `localhost:4000` 能正常显示页面和调用 AI，说明部署配置正确。
