# 互动式小说 PRD 评审优化建议

> 基于《互动式小说产品需求交互设计文档（PRD）- V1.1》及补充产品思路整理。  
> 评审重点：产品范围收敛、AI 生成链路、缓存规则、思维导图交互、MVP 可落地性。

## 一、整体结论

当前 PRD 的大方向是成立的，尤其是以下几点已经比较明确：

- 以知乎盐言故事为基础，提供“可以选择剧情的互动式小说”体验。
- 进入正文后优先保障阅读体验，AI 节点分析、世界观生成、关系网生成在后台静默执行。
- 知乎接口存在调用限制，因此列表和详情内容都采用强制本地 24 小时缓存。
- 详情页围绕正文阅读、关键节点、分支选择、思维导图和番外/续写形成完整主流程。
- 刘看山形象作为轻量引导，能增强产品识别度和陪伴感。

但这版 PRD 目前的问题不是功能不够，而是功能边界偏大、部分规则存在冲突、AI 输出协议不够明确。建议下一版重点补充“实现约束”，把 P0 范围压实，避免开发阶段出现大量解释成本。

核心建议：

> 第一版不要追求完整的世界观系统和复杂导图编辑能力，而应优先验证“用户在正文关键节点做选择，并获得即时分支反馈”的核心闭环。

## 二、已经做得好的地方

### 1. 正文优先的体验原则是正确的

PRD 明确提出进入详情页后，正文应立即展示，AI 分析在后台静默执行，不用 Loading 阻塞用户阅读。这一点非常重要。

对于阅读类产品，首要体验是“能不能马上读”。如果一进入详情页就等待 AI 提炼节点、生成世界观、生成关系网，用户会明显感到被打断。

建议保留当前原则：

- 正文内容优先展示。
- AI 关键节点生成只用轻量提示。
- 世界观、人物小传、关系网默认不打扰用户。
- 只有用户主动打开思维导图或人物志时，再展示更详细的 AI 分析状态。

### 2. 24 小时本地缓存方向正确

由于知乎接口有调用限制，PRD 中对列表和详情内容都设计了强制本地缓存，这是必要的。

建议保留：

- 列表缓存：`yyan_story_list_v1`
- 详情缓存：`yyan_story_detail_{work_id}`
- 缓存字段：`data`、`cached_at`、`expires_at`
- 24 小时内默认只读本地缓存，不重复调用远程接口。

但后续需要补充“例外规则”，见后文。

### 3. 主流程已经完整

当前 PRD 已覆盖：

- 列表页
- 搜索
- 详情页
- 正文展示
- 关键节点高亮
- AI 分支选择
- 自定义输入
- 分支生成
- 思维导图
- 番外/续写
- 异常处理
- 后续迭代路线图

整体已经具备进入原型设计和技术方案设计的基础。

## 三、最需要优化的 5 个问题

## 1. 缓存规则存在冲突，需要统一

PRD 中一处强调：

> 在 24 小时内，任何刷新或重新进入页面的操作，只从本地缓存读取数据，不再发起任何网络请求。

但后文又提到：

- 用户可以点击“立即更新”按钮，强制跳过缓存。
- API 调用失败时，重试按钮可以强制发起新请求。
- 缓存损坏时，可以清除缓存并重新获取。

这些规则本身都合理，但目前写法会让技术实现产生歧义：到底是“绝不绕过缓存”，还是“特殊场景可以绕过缓存”？

建议改成：

### 推荐规则

默认规则：

- 24 小时内，列表和详情内容默认只读本地缓存。
- 普通刷新、重新进入页面、下拉刷新都不触发远程 API。

允许例外：

- 缓存不存在。
- 缓存已过期。
- 缓存损坏或结构不合法。
- 产品明确开放“立即更新”能力，且该能力受到开关控制。
- 管理员或调试模式允许强制刷新。

不建议在普通用户侧默认提供“立即更新”，否则会削弱强制缓存策略。

## 2. MVP 的思维导图交互过重

当前 PRD 中思维导图包含：

- 常规模式
- 全屏模式
- 点击节点定位正文
- 双击查看详情卡片
- 右键快捷菜单
- 拖动节点自定义布局
- 节点搜索
- 分支切换
- 布局状态保存
- 世界观人物志面板

这些功能完整，但对 MVP 来说偏重，尤其是右键、拖动、自定义布局、分支版本切换，会明显增加开发和测试成本。

### 建议 P0 保留

- 右上角小型思维导图。
- 点击全屏查看。
- 点击节点定位到正文对应段落。
- 分支生成后，在对应节点下新增子节点。
- 展示当前选择路径。
- 展示番外/续写节点。

### 建议 P1 或以后再做

- 右键菜单。
- 双击节点详情卡片。
- 拖动节点自定义布局。
- 节点搜索。
- 用户编辑节点。
- 多版本复杂切换。
- 分支热力图。

思维导图在第一版的核心职责应是：

> 总览、定位、切换分支。

不要让它过早变成一个复杂的图编辑器。

## 3. AI 输出缺少结构化协议

PRD 已经说明 AI 要生成：

- 宏观世界观
- 分场景世界观
- 人物小传
- 关系网
- 关键节点
- 3 个分支选项
- 番外方向
- 续写方向
- 分支剧情

但目前还缺少明确的数据结构。若不提前定义，前端会很难稳定渲染高亮、思维导图和分支记录。

建议下一版 PRD 增加 AI 输出 JSON Schema。

### 示例：关键节点 KeyNode

```json
{
  "node_id": "node_001",
  "title": "女主发现真相",
  "summary": "主角在对话中发现关键证据，剧情发生转折。",
  "importance": "main",
  "node_type": "turning_point",
  "paragraph_index": 12,
  "char_range": [20, 48],
  "anchor_text": "她终于意识到，所有线索都指向同一个人。",
  "quote_hash": "sha256_xxx",
  "branch_options": [
    {
      "option_id": "opt_001",
      "text": "立刻质问对方",
      "tone": "冲突升级"
    },
    {
      "option_id": "opt_002",
      "text": "假装不知道，继续调查",
      "tone": "悬疑推进"
    },
    {
      "option_id": "opt_003",
      "text": "向朋友求助",
      "tone": "关系扩展"
    }
  ]
}
```

### 示例：分支 Branch

```json
{
  "branch_id": "branch_001",
  "source_node_id": "node_001",
  "choice_type": "preset",
  "choice_text": "假装不知道，继续调查",
  "status": "success",
  "generated_content": "她压下心里的震惊，装作若无其事地移开视线……",
  "created_at": 1778550000000,
  "model": "deepseek",
  "prompt_version": "v1"
}
```

### 示例：思维导图节点 MindMapNode

```json
{
  "id": "map_node_001",
  "parent_id": "root",
  "type": "branch",
  "label": "假装不知道",
  "summary": "主角选择继续调查，进入隐藏线。",
  "target_paragraph_index": 12,
  "branch_id": "branch_001"
}
```

### 示例：故事分析 StoryAnalysis

```json
{
  "work_id": "1644038836790169600",
  "content_hash": "sha256_xxx",
  "model": "deepseek",
  "prompt_version": "v1",
  "world_summary": "故事发生在近未来都市，核心矛盾是身份伪装与信任崩塌。",
  "characters": [
    {
      "character_id": "char_001",
      "name": "林夏",
      "role": "主角",
      "personality": "敏感、谨慎、行动力强",
      "motivation": "寻找真相并保护自己"
    }
  ],
  "locations": [],
  "objects": [],
  "relations": []
}
```

## 4. 分支内容的追加位置需要统一

PRD 中有两种表述：

- 分支生成后，正文区域可实时滚动至生成的剧情段落。
- 任何分支生成的内容同时追加到当前节点下方的正文末尾。
- 番外/续写生成后追加到当前正文末尾。

这里会产生一个核心问题：

> 分支内容到底是插入原文中对应节点附近，还是统一追加到正文末尾？

这会直接影响：

- 阅读流是否连续。
- 思维导图如何定位。
- 多个分支是否同时展示。
- 切换分支时正文如何变化。

### 推荐方案

将正文分为两类内容：

1. 原始正文  
   永远保持不变，不直接改写。

2. AI 衍生内容  
   作为分支块插入或展示，但数据上独立保存。

交互上建议：

- 点击某个关键节点生成分支后，可以在该节点下方临时插入“AI 分支内容块”。
- 同时右侧分支面板保留生成记录。
- 番外/续写默认追加到正文末尾的“AI 衍生内容区”。
- 切换分支时，只切换当前可见的 AI 分支块，不改变原始正文。

这样可以避免原文和 AI 生成内容混在一起，方便回滚和版本管理。

## 5. AI 成本与任务优先级需要单独设计

当前 PRD 中，进入详情页后后台会并行生成：

- 关键节点
- 世界观
- 人物小传
- 关系网
- 分支预设方向

如果故事较长，这会带来明显成本和延迟压力。尤其每次详情页都触发全量分析，会产生大量重复调用。

### 建议任务分层

P0 必须生成：

- 关键节点
- 每个关键节点的 2-3 个分支选项
- 基础故事摘要

P0 可后台低优先级生成：

- 宏观世界观
- 主要人物简表

P1 再生成：

- 完整人物小传
- NPC 小传
- 物品小传
- 关系网
- 分场景、分时段世界观

按需生成：

- 用户点击某个人物后，再生成该人物深度小传。
- 用户打开世界观面板后，再补充更完整的设定。
- 用户触发番外/续写时，再调用更长上下文。

### 建议 AI 缓存

除知乎接口缓存外，还应缓存 AI 结果：

```json
{
  "cache_key": "ai_analysis_{work_id}_{content_hash}_{prompt_version}_{model}",
  "work_id": "1644038836790169600",
  "content_hash": "sha256_xxx",
  "prompt_version": "v1",
  "model": "deepseek",
  "data": {},
  "cached_at": 1778550000000
}
```

这样可以避免重复分析同一篇故事。

## 四、建议的 P0 范围

### 列表页

保留：

- 获取故事概要列表。
- 本地 24 小时缓存。
- 标题、简介、标签展示。
- 按标题和简介实时搜索。
- 点击进入详情页。
- 刘看山轻量引导。

暂缓：

- 复杂推荐。
- 分频道筛选。
- 个性化排序。
- 上拉加载更多，除非接口本身分页。

### 详情页

保留：

- 正文展示。
- 作者、标签、导语。
- 正文按段落解析。
- 右上角思维导图。
- AI 关键节点后台生成。
- 节点生成完成后轻量提示。
- 关键节点高亮。
- 点击节点展示分支选择。

暂缓：

- 复杂三栏固定布局。
- 过多颜色等级的节点高亮。
- 用户手动编辑节点。
- 全量人物关系网默认展示。

### 分支交互

保留：

- 3 个 AI 预设选项。
- 1 个自定义输入框。
- 选择后生成分支剧情。
- 右侧分支记录卡片。
- 思维导图新增分支节点。
- 失败重试。

建议调整：

- 节点预设选项可以是 2-3 个，不必强制每次都是 3 个。
- 分支生成失败后，不应影响原文阅读。
- 分支记录默认保留最近 30 条可以保留，但需要提供清理机制。

### 思维导图

保留：

- 右上角小图。
- 全屏展开。
- 点击节点定位正文。
- 展示分支树。
- 展示番外/续写节点。

暂缓：

- 右键菜单。
- 拖动布局。
- 双击详情。
- 复杂版本切换。
- 节点搜索。

### 番外/续写

保留：

- 正文末尾展示“番外”“续写”按钮。
- 点击后给出 AI 预设方向。
- 支持自定义输入。
- 生成后追加到 AI 衍生内容区。
- 同步追加到思维导图。

建议：

- 番外和续写底层复用同一个 `Branch` 模型。
- 区别只体现在 `branch_type`：
  - `node_branch`
  - `extra`
  - `continuation`

## 五、模型选择建议

PRD 中选择 DeepSeek 为主、豆包为备是合理的。

建议策略：

- DeepSeek 作为默认模型，用于关键节点提炼、结构化输出、剧情分支生成。
- 豆包作为备选，用于 DeepSeek 限流、超时、内容安全链路更适合时降级。
- 业务侧不要直接依赖某个模型接口，应封装统一模型适配层。

### 模型适配层建议

统一接口可以抽象为：

```ts
interface StoryAIProvider {
  analyzeStory(input: AnalyzeStoryInput): Promise<StoryAnalysis>;
  generateKeyNodes(input: KeyNodeInput): Promise<KeyNode[]>;
  generateBranchOptions(input: BranchOptionInput): Promise<BranchOption[]>;
  generateBranchContent(input: BranchGenerateInput): Promise<BranchResult>;
  generateExtraOrContinuation(input: ExtraInput): Promise<BranchResult>;
}
```

这样后续切换 DeepSeek、豆包或其他模型时，不需要修改业务层。

## 六、交互体验建议

### 1. 关键节点高亮要克制

不要把所有 AI 识别出的节点都在正文里高亮。

建议：

- 默认只展示 3-8 个高置信关键节点。
- 主节点使用明显高亮。
- 次级节点只在思维导图或全屏模式中展示。
- 用户滚动阅读时，不要频繁出现动效。

### 2. 刘看山引导要避免过度打扰

刘看山适合做品牌化陪伴，但不能频繁弹气泡。

建议：

- 列表页首次进入可出现一次。
- 详情页阅读中只在关键节点生成完成、分支生成完成、异常失败时出现。
- 用户关闭后，本次会话不要反复弹出。
- 深夜提醒、沉迷提醒可以保留，但语气要克制。

### 3. 分支生成 Loading 要和阅读解耦

用户选择分支后，右侧卡片展示 Loading 即可，不要阻塞正文滚动。

建议状态：

- `pending`
- `generating`
- `success`
- `failed`
- `cancelled`

失败时提供：

- 重新生成。
- 修改输入。
- 删除记录。

## 七、安全与合规建议

PRD 已提到 AI 拟人化、沉迷和心理健康保护，这是对的。建议进一步补充：

- AI 内容安全审核。
- 敏感内容过滤。
- 未成年人保护提示。
- 生成内容免责声明。
- 高危情绪识别后的降级回复。
- 连续高频互动后的休息提醒。
- 用户自定义输入的安全检测。

同时需要注意番外/续写的版权边界：

- 不应暗示 AI 生成内容是原作者创作。
- 应明确标注为 AI 衍生内容。
- 分享时应区分原文内容和 AI 生成内容。

## 八、下一版 PRD 建议补充章节

建议在 V1.2 中新增以下章节：

1. AI 输出 JSON Schema
2. 分支版本状态模型
3. 缓存例外规则
4. AI 成本预算与调用策略
5. P0 / P1 / P2 功能边界
6. 内容安全与版权边界
7. 埋点与评估指标

## 九、建议关注的核心指标

上线后建议重点看：

- 详情页正文打开成功率。
- AI 关键节点生成成功率。
- 关键节点点击率。
- 分支选项点击率。
- 自定义输入使用率。
- 分支生成成功率。
- 分支生成平均耗时。
- 番外/续写点击率。
- 用户平均阅读时长。
- 单篇故事平均分支数。
- 思维导图全屏打开率。
- 分支内容删除/重生成率。

这些指标可以帮助判断：

- 用户是否真的愿意互动。
- AI 生成质量是否足够。
- 思维导图是否有价值。
- 番外/续写是否比普通分支更有吸引力。

## 十、最终建议

当前 PRD 已经可以作为方向版方案，但还不适合作为直接开发依据。建议下一步产出一版“实现约束版 PRD”，重点不是新增功能，而是做减法和定规则。

建议优先级：

1. 先确定 P0 范围。
2. 补齐 AI 输出 schema。
3. 统一缓存强制规则和例外规则。
4. 明确分支内容插入与版本切换模型。
5. 补 AI 成本和安全策略。
6. 再进入原型和技术设计。

一句话总结：

> 这个产品方向最有价值的不是“AI 帮故事生成很多设定”，而是“读者在关键时刻做选择，并看到属于自己的故事走向”。第一版应该集中火力把这个闭环做顺。
